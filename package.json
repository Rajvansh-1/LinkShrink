{
  "name": "linkshrink-monorepo",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd client && npm install && cd ../server && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\" --names \"SERVER,CLIENT\" --prefix-colors \"magenta,cyan\"",
    "build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": ">=20.x"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nanoid": "^3.3.7",
    "valid-url": "^1.0.9",
    "@libsql/client": "^0.15.15"
  }
}